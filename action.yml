name: "AWS S3 sync with Parcel using private npm packages"
description: "Builds with Parcel and private packages either private npm or GitHub packages in a private org"
author: brockmdavis
runs:
  using: docker
  image: Dockerfile
branding:
  icon: refresh-cw
  color: green
