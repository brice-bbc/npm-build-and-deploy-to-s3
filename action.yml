name: "Publish to AWS S3"
description: "Builds with private packages either private npm or GitHub packages in a private org"
author: brice-bbc
runs:
  using: docker
  image: Dockerfile
branding:
  icon: refresh-cw
  color: green
